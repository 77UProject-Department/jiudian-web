# Paste into this site's Nginx rewrite settings (server context).
# If a location / block already exists, edit it instead of adding a duplicate.
location / {
    index index.html;
    try_files $uri $uri/ =404;
}
